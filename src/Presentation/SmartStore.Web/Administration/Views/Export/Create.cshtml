@using SmartStore.Admin.Models.DataExchange
@model ExportProfileModel
@{
	Layout = null;
}

<div style="padding-bottom: 5px;">
	@if(Model.Providing.AvailableExportProviders.Count > 0)
	{
		<form method="POST" class="export-profile-form" action="@Url.Action("Create", "Export")">
			<table class="adminContent">
				<tr>
					<td class="adminTitle">
						@Html.SmartLabelFor(model => model.Providing.SystemName)
					</td>
					<td class="adminData">
						@Html.DropDownListFor(model => model.Providing.SystemName, Model.Providing.AvailableExportProviders)
					</td>
				</tr>
			</table>
		</form>
	}
	else
	{
		@T("Admin.Configuration.Export.NoProfiles")
	}
</div>