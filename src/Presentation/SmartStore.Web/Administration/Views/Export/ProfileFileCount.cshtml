@using SmartStore.Admin.Models.DataExchange;
@model ExportFileCountModel
@if (Model.HasItems)
{
	<a href="javascript:void(0)" class="export-file-count">@Model.FileCount</a>
}
else
{
	<span title="@T("Common.NoFilesAvailable")">0</span>
}