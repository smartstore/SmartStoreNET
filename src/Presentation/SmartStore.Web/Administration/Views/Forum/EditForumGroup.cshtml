@model ForumGroupModel
@{
    ViewBag.Title = T("Admin.ContentManagement.Forums.ForumGroup.EditForumGroup").Text;
}
@using (Html.BeginForm())
{
    <div class="section-header">
        <div class="title">
            <i class="fa fa-users"></i>
            @T("Admin.ContentManagement.Forums.ForumGroup.EditForumGroup") - @Model.Name  @Html.ActionLink(String.Format("({0})", T("Admin.ContentManagement.Forums.ForumGroup.BackToList")), "List")
        </div>
        <div class="options">
			@Html.Widget("admin_button_toolbar_before")

            <button type="submit" name="save" value="save" class="btn btn-primary"><i class="fa fa-check"></i>&nbsp;@T("Admin.Common.Save")</button>
            <button type="submit" name="save" value="save-continue" class="btn">@T("Admin.Common.SaveContinue")</button>
            <button type="submit" name="forumgroup-delete" value="forumgroup-delete" id="forumgroup-delete" class="btn btn-danger"><i class="fa fa-trash-o"></i>&nbsp;@T("Admin.Common.Delete")</button>

			@Html.Widget("admin_button_toolbar_after")
        </div>
    </div>
    @Html.Partial("_CreateOrUpdateForumGroup", Model)
}
@Html.DeleteConfirmation("DeleteForumGroup", "forumgroup-delete")
