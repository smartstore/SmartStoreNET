@model ManufacturerModel
@{
    //page title
    ViewBag.Title = T("Admin.Catalog.Manufacturers.AddNew").Text;
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "manufacturer-form" }))
{
    <div class="section-header">
        <div class="title">
            @T("Admin.Catalog.Manufacturers.AddNew") @Html.ActionLink("(" + T("Admin.Catalog.Manufacturers.BackToList") + ")", "List")
        </div>
        <div class="options">
            <button type="submit" name="save" value="save" class="btn btn-primary"><i class="icon-ok icon-white"></i>&nbsp;@T("Admin.Common.Save")</button>
            <button type="submit" name="save" value="save-continue" class="btn">@T("Admin.Common.SaveContinue")</button>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}