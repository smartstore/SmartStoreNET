@model ProductModel
@{
    //page title
    ViewBag.Title = T("Admin.Catalog.Products.AddNew").Text;
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "product-form" }))
{
    <div class="section-header">
        <div class="title">
            @T("Admin.Catalog.Products.AddNew") @Html.ActionLink("(" + T("Admin.Catalog.Products.BackToList") + ")", "List")
        </div>
        <div class="options">
            <button type="submit" name="save" value="save" class="btn btn-primary entity-commit-trigger" data-commit-type="product" data-commit-action="insert" data-commit-id="@Model.Id">
                <i class="icon-ok"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save" value="save-continue" class="btn entity-commit-trigger" data-commit-type="product" data-commit-action="insert" data-commit-id="@Model.Id">
                @T("Admin.Common.SaveContinue")
            </button>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}