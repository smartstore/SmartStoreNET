@model ShopHeaderModel

@using SmartStore.Web.Models.Common;

@Html.Action("Menu", "Common")

@*<div id="logobar" class="container clearfix">

    <a href="@Url.RouteUrl("HomePage")" class="pull-left brand@(Model.LogoUploaded ? "" : " textual")">
            
        @if (Model.LogoUploaded) 
        {
            <img src='@Url.Content(Model.LogoUrl)' 
                alt="@Model.LogoTitle" 
                title="@Model.LogoTitle"
                @Html.Attr("width", Model.LogoWidth.ToString(), Model.LogoWidth > 0)
                @Html.Attr("height", Model.LogoHeight.ToString(), Model.LogoHeight > 0) />
        }
        else
        {
            @Model.LogoTitle    
        }
    </a>

    <nav id="nav-home pull-left" class="clearfix">
        @Html.Action("Menu", "Common")
    </nav>

    <div class="regional-selectors pull-left">
        @Html.Action("LanguageSelector", "Common")
        @Html.Action("CurrencySelector", "Common")
    </div>
</div>*@