@model EditForumTopicModel
@using SmartStore.Web.Models.Boards;
@{    
    Layout = "_Layout";

    Html.AddTitleParts(T("Forum.PageTitle.TopicNew").Text);
}
@{ Html.RenderPartial("_CreateUpdateTopic", @Model); }
