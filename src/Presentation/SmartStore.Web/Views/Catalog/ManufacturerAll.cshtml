@using SmartStore.Core;
@using SmartStore.Core.Infrastructure;
@using SmartStore.Web.Models.Catalog;
@model IList<ManufacturerModel>
@{
    Layout = "_ColumnsTwo";

    Html.AddTitleParts(T("PageTitle.Manufacturers").Text);
}
<div class="page manufacturer-list-page">
    <div class="page-title">
        <h1>@T("Manufacturers.List")</h1>
    </div>
    <div class="page-body">
        <div class="card-deck card-cols-sm-1 card-cols-md-2 card-cols-lg-3 manufacturer-grid">
            @if (Model.Count > 0)
            {

                foreach (var item in Model)
                {
                    <div class="card card-block card-shadow">
                        <h2 class="card-title">
                            <a href="@Url.RouteUrl("Manufacturer", new { SeName = item.SeName })" title="@item.PictureModel.Title">@item.Name</a>
                        </h2>
                        <div class="picture">
                            @if (item.PictureModel.ImageUrl.HasValue())
                            {
                                <a href="@Url.RouteUrl("Manufacturer", new { SeName = item.SeName })" title="@item.PictureModel.Title">
                                    <img alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title" />
                                </a>
                            }
                        </div>
                    </div>
                }
            }
        </div>
    </div>
</div>
