@model CheckoutCompletedModel
@using SmartStore.Core.Domain.Orders;
@using SmartStore.Web.Models.Checkout;
@using SmartStore.Web.Models.Common;
@{
    Layout = "~/Views/Shared/_Root.cshtml";

    Html.AddTitleParts(T("PageTitle.Checkout").Text);
}
<div class="page checkout-page">
    <div class="page-title">
        <h1>@T("Checkout.ThankYou")</h1>
    </div>
    <div class="page-body checkout-data">
        <div class="order-completed">
            @Html.Widget("mobile_checkout_completed_top")
            <div class="body">
                <strong>
					@T("Checkout.OrderHasBeenReceived")
				</strong>
                <p>
                    @T("Checkout.OrderNumber"): @Model.OrderNumber
                </p>
                <p>
					<a href='@Url.Action("Details", "Order", new { id = Model.OrderId })' data-role="button" rel="nofollow">@T("Checkout.PlacedOrderDetails")</a>
                </p>
            </div>
            <div class="select-button">
                <a href="@Url.RouteUrl("HomePage")" data-role="button">@T("Checkout.Continue")</a>
            </div>
            @Html.Widget("mobile_checkout_completed_bottom")
        </div>
    </div>
</div>
