@model MediaTemplateModel

@using SmartStore.Web.Framework.Modelling;

<img src="@(Model.RenderViewer ? Model.File.Url : Model.File.ThumbUrl)"
     class="@("file-img".Grow(Model.ExtraCssClasses, " "))"
     alt="@Model.Alt"
     title="@Model.Title" />